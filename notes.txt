project directory: where you work 
repository: hidden in pd, as .git. stores ALL version histories.
commit: 현재의 모습(pd)을 캡쳐해서 repo에 저장. 혹은 저장된 것. --> repo에 저장됨

git init: 시작하겠다 -> repo생성.

commit하기 전에 누가 했는지 남겨주는 것 --> 이름과 이멜 

git config user.name "이름"
git config user.email "이메일"
